# 🔧 Решение проблем при установке

## Проблема: Ошибка установки pymssql

### Ошибка:
```
Failed building wheel for pymssql
CompileError: src\pymssql\_mssql.pyx
```

### Решение:
`pymssql` не поддерживает Python 3.13. Он закомментирован в `requirements.txt`.

**Если нужен SQL Server:**
1. Используйте `pyodbc` вместо `pymssql`:
   ```bash
   pip install pyodbc
   ```
2. Установите ODBC драйвер для SQL Server: https://learn.microsoft.com/en-us/sql/connect/odbc/download-odbc-driver-for-sql-server

**Или используйте PostgreSQL** (рекомендуется) - `psycopg2-binary` работает отлично.

---

## Проблема: Ошибка установки psycopg2-binary

### Ошибка:
```
Error: pg_config executable not found.
ERROR: Failed to build 'psycopg2-binary'
```

### Решение 1: Использовать скрипт установки
```bash
install_deps.bat
```

### Решение 2: Установить вручную

```bash
# 1. Обновить pip и setuptools
python -m pip install --upgrade pip setuptools wheel

# 2. Установить psycopg2-binary отдельно
python -m pip install --upgrade --force-reinstall --no-cache-dir psycopg2-binary

# 3. Установить остальные зависимости
pip install -r requirements.txt
```

### Решение 3: Использовать альтернативу (если PostgreSQL не нужен)

Если вы используете только SQL Server, можно временно закомментировать psycopg2-binary:

В `requirements.txt` закомментируйте:
```txt
# psycopg2-binary>=2.9.9
```

И установите зависимости:
```bash
pip install -r requirements.txt
```

### Решение 4: Установить PostgreSQL (если нужен)

1. Скачайте PostgreSQL: https://www.postgresql.org/download/windows/
2. Установите PostgreSQL
3. Добавьте `bin` директорию PostgreSQL в PATH
4. Попробуйте установить зависимости снова

---

## Проблема: "Module not found"

### Решение:
```bash
# Убедитесь, что виртуальное окружение активировано
venv\Scripts\activate

# Переустановите зависимости
pip install -r requirements.txt
```

---

## Проблема: "Port already in use"

### Решение:
```powershell
# Найти процесс на порту
netstat -ano | findstr :8001

# Убить процесс (замените <PID> на номер процесса)
taskkill /PID <PID> /F
```

---

## Проблема: "Database connection error"

### Решение:
1. Проверьте настройки в `.env`
2. Убедитесь, что БД запущена
3. Проверьте права доступа
4. Проверьте формат `DATABASE_URL`:
   - PostgreSQL: `postgresql://user:password@localhost:5432/dbname`
   - SQL Server: используйте `SQL_SERVER_CONNECTION` в `.env`

---

## Проблема: "Import errors" при запуске

### Решение:
```bash
# Убедитесь, что вы запускаете из правильной директории
cd backend_fastapi

# Или установите проект как пакет
pip install -e .
```

---

## Проблема: "JWT secret key not found"

### Решение:
Создайте `.env` файл с:
```env
JWT_SECRET_KEY=your-super-secret-key-min-32-chars-long
JWT_ISSUER=choice-api
JWT_AUDIENCE=choice-app
```

---

## Проблема: "Cannot connect to RabbitMQ"

### Решение:
1. Установите RabbitMQ: https://www.rabbitmq.com/download.html
2. Или закомментируйте RabbitMQ зависимости в коде (если не нужен)

---

## Общие советы

1. **Всегда активируйте виртуальное окружение** перед установкой
2. **Обновляйте pip** регулярно: `python -m pip install --upgrade pip`
3. **Используйте Python 3.11+** для совместимости
4. **Проверяйте логи** при ошибках

---

## Если ничего не помогает

1. Удалите виртуальное окружение:
```bash
rmdir /s venv
```

2. Создайте новое:
```bash
python -m venv venv
venv\Scripts\activate
```

3. Обновите pip:
```bash
python -m pip install --upgrade pip setuptools wheel
```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```
