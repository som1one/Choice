# 🚀 Как запустить и проверить все сервисы

## 📋 Шаг 1: Установка

```bash
cd backend_fastapi

# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

---

## ⚙️ Шаг 2: Настройка .env

Создайте файл `.env` в папке `backend_fastapi/`:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/choice_db
JWT_SECRET_KEY=your-super-secret-key-min-32-chars
JWT_ISSUER=choice-api
JWT_AUDIENCE=choice-app
```

**Важно:** Замените `user`, `password` и `localhost` на ваши данные БД!

---

## 🏃 Шаг 3: Запуск всех сервисов

### Windows:
**В PowerShell:**
```powershell
.\start_all.bat
```

**Или в CMD:**
```cmd
start_all.bat
```

### Linux/Mac:
```bash
chmod +x start_all.sh
./start_all.sh
```

**Или вручную** (откройте 8 терминалов):

```bash
# Терминал 1
cd services/authentication
uvicorn main:app --port 8001 --reload

# Терминал 2
cd services/client_service
uvicorn main:app --port 8002 --reload

# Терминал 3
cd services/company_service
uvicorn main:app --port 8003 --reload

# Терминал 4
cd services/category_service
uvicorn main:app --port 8004 --reload

# Терминал 5
cd services/ordering
uvicorn main:app --port 8005 --reload

# Терминал 6
cd services/chat
uvicorn main:app --port 8006 --reload

# Терминал 7
cd services/review_service
uvicorn main:app --port 8007 --reload

# Терминал 8
cd services/file_service
uvicorn main:app --port 8008 --reload
```

---

## ✅ Шаг 4: Проверка работы

### 1. Проверка через скрипт:
```bash
python check_services.py
```

Должно показать:
```
✅ Authentication  - OK
✅ Client         - OK
✅ Company        - OK
...
```

### 2. Проверка через браузер:

Откройте в браузере:
- http://localhost:8001/docs - Authentication Service (Swagger)
- http://localhost:8004/docs - Category Service (Swagger)
- http://localhost:8001/health - Health check

### 3. Тестирование API:

```bash
python test_api.py
```

Этот скрипт:
1. Зарегистрирует тестового пользователя
2. Выполнит вход
3. Получит категории

---

## 🧪 Тестирование через Swagger UI

1. Откройте http://localhost:8001/docs
2. Попробуйте endpoint `/api/auth/register`:
   - Нажмите "Try it out"
   - Введите данные:
     ```json
     {
       "email": "test@example.com",
       "name": "Тест",
       "password": "Test1234!",
       "street": "Ленина",
       "city": "Москва",
       "phone_number": "1234567890",
       "type": "Client"
     }
     ```
   - Нажмите "Execute"
3. Если получили ответ 200 - всё работает! ✅

---

## 🔍 Проверка конкретного сервиса

### Authentication Service:
```bash
curl http://localhost:8001/health
# Должно вернуть: {"status": "healthy"}
```

### Category Service:
```bash
# Сначала получите токен через /api/auth/login
TOKEN="your-token-here"
curl -H "Authorization: Bearer $TOKEN" http://localhost:8004/api/category/get
```

---

## 🐛 Если что-то не работает

### Ошибка: "Module not found"
```bash
# Убедитесь, что виртуальное окружение активировано
venv\Scripts\activate  # Windows
# Переустановите зависимости
pip install -r requirements.txt
```

### Ошибка: "Port already in use"
```bash
# Windows: найти и убить процесс
netstat -ano | findstr :8001
taskkill /PID <номер> /F

# Linux/Mac:
lsof -i :8001
kill -9 <PID>
```

### Ошибка: "Database connection error"
- Проверьте настройки в `.env`
- Убедитесь, что PostgreSQL/SQL Server запущен
- Проверьте права доступа к БД

### Ошибка: "Import errors"
```bash
# Убедитесь, что вы в правильной директории
cd backend_fastapi

# Или установите проект как пакет
pip install -e .
```

---

## 📊 Порты сервисов

| Сервис | Порт | Swagger UI |
|--------|------|------------|
| Authentication | 8001 | http://localhost:8001/docs |
| Client | 8002 | http://localhost:8002/docs |
| Company | 8003 | http://localhost:8003/docs |
| Category | 8004 | http://localhost:8004/docs |
| Ordering | 8005 | http://localhost:8005/docs |
| Chat | 8006 | http://localhost:8006/docs |
| Review | 8007 | http://localhost:8007/docs |
| File | 8008 | http://localhost:8008/docs |

---

## ✅ Чеклист

- [ ] Python 3.11+ установлен
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] `.env` файл создан и настроен
- [ ] База данных создана и доступна
- [ ] Все сервисы запущены
- [ ] `python check_services.py` показывает все ✅
- [ ] Swagger UI открывается в браузере
- [ ] Тестовый запрос выполнен успешно

---

## 🎯 Готово!

Если все чек-листы пройдены - сервисы работают! 🎉

**Следующие шаги:**
1. Протестировать все endpoints через Swagger
2. Настроить RabbitMQ (если нужно)
3. Настроить реальные API интеграции
