# ⚠️ Важно перед запуском

## ✅ Что исправлено:

1. **SQLAlchemy обновлен** до версии 2.0.46 (совместимость с Python 3.13)
2. **Импорты исправлены** - теперь работают при запуске из корня
3. **Настройки Pydantic** - добавлен `extra = "ignore"` для всех Settings классов
4. **Батник исправлен** - теперь запускает из корневой директории с правильными путями

## 🚀 Как запустить:

### 1. Убедитесь, что БД запущена
PostgreSQL должен быть запущен и доступен по адресу из `.env`

### 2. Запустите сервисы:
```powershell
.\start_all.bat
```

### 3. Если окна пустые:
- Проверьте, что виртуальное окружение активировано
- Проверьте настройки в `.env`
- Проверьте, что БД запущена

### 4. Для тестирования одного сервиса:
```powershell
.\start_one.bat authentication 8001
```

## 📝 Примечания:

- Ошибка подключения к БД при импорте - это нормально, если БД не запущена
- Сервисы будут пытаться подключиться к БД при первом запросе
- Если БД не настроена, создайте её или используйте SQLite для тестирования
