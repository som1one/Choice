# ✅ Установка завершена успешно!

Все зависимости установлены. Теперь можно запускать сервисы.

## 🚀 Следующие шаги:

### 1. Настройте .env файл
Создайте `.env` в папке `backend_fastapi/`:
```env
DATABASE_URL=postgresql://user:password@localhost:5432/choice_db
JWT_SECRET_KEY=your-super-secret-key-min-32-chars
JWT_ISSUER=choice-api
JWT_AUDIENCE=choice-app
```

### 2. Запустите сервисы
```bash
start_all.bat
```

### 3. Проверьте работу
```bash
python check_services.py
```

Или откройте в браузере:
- http://localhost:8001/docs

---

## 📝 Примечания:

- **pymssql** не установлен (не поддерживает Python 3.13)
- Если нужен SQL Server, используйте `pyodbc` (см. `requirements-optional.txt`)
- Для PostgreSQL используйте `psycopg2-binary` (уже установлен)

---

## ✅ Готово к работе!
