# ⚡ Простой старт (для Windows)

## 1️⃣ Установка (один раз)

```powershell
cd backend_fastapi
python -m venv venv
venv\Scripts\activate

# Обновить pip
python -m pip install --upgrade pip setuptools wheel

# Установить зависимости
pip install -r requirements.txt
```

**Примечание:** Если возникнут проблемы, используйте `install_deps.bat`

## 2️⃣ Настройка .env

Создайте файл `.env` в папке `backend_fastapi/`:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/choice_db
JWT_SECRET_KEY=my-super-secret-key-12345678901234567890
JWT_ISSUER=choice-api
JWT_AUDIENCE=choice-app
```

## 3️⃣ Запуск

**⚠️ ВАЖНО: В PowerShell ВСЕГДА используйте `.\` перед скриптом!**

**В PowerShell:**
```powershell
.\start_all.bat
```

**Или в обычном CMD:**
```cmd
start_all.bat
```

**Если забыли `.\` - PowerShell покажет ошибку.**

Откроется 8 окон - по одному для каждого сервиса.

## 4️⃣ Проверка

Откройте в браузере:
- http://localhost:8001/docs - Swagger UI

Или запустите:
```powershell
python check_services.py
```

---

## ✅ Готово!

Если видите Swagger UI - всё работает! 🎉
