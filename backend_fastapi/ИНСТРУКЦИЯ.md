# 📖 Инструкция по запуску и проверке

## 🎯 Самый простой способ

### 1. Установка
```bash
cd backend_fastapi
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Создать .env
Создайте файл `.env` с настройками БД (см. `START_GUIDE.md`)

### 3. Запустить все сервисы

**В PowerShell:**
```powershell
.\start_all.bat
```

**Или запустить один сервис для проверки:**
```powershell
cd services/authentication
python -m uvicorn main:app --port 8001 --reload
```

### 4. Проверить
Откройте http://localhost:8001/docs в браузере

Если видите Swagger UI - всё работает! ✅

---

## 📚 Документация

- **`ПРОСТОЙ_СТАРТ.md`** - Самый простой способ (Windows)
- **`КАК_ЗАПУСТИТЬ.md`** - Подробная инструкция
- **`START_GUIDE.md`** - Полное руководство
- **`TESTING.md`** - Руководство по тестированию

---

## ⚠️ Важно

1. **База данных должна быть запущена** (PostgreSQL или SQL Server)
2. **.env файл должен быть настроен** с правильными данными БД
3. **Виртуальное окружение должно быть активировано**

---

## 🐛 Если не работает

1. Проверьте, что Python 3.11+ установлен
2. Проверьте, что виртуальное окружение активировано
3. Проверьте, что все зависимости установлены
4. Проверьте настройки в `.env`
5. Проверьте, что БД запущена и доступна

---

## ✅ Готово!

После успешного запуска вы увидите:
- Swagger UI в браузере
- Логи в консоли
- Health check возвращает 200

**Всё работает!** 🎉
