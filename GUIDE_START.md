# 🚀 Гайд по запуску приложения

## 📋 Содержание
1. [Требования](#требования)
2. [Установка зависимостей](#установка-зависимостей)
3. [Запуск на Android (Windows/Linux/macOS)](#запуск-на-android)
4. [Запуск на iOS (только macOS)](#запуск-на-ios)
5. [Решение проблем](#решение-проблем)

---

## 📦 Требования

### Общие требования:
- **Node.js** >= 18.x
- **npm** или **yarn**
- **Git**

### Для Android (Windows/Linux/macOS):
- **Java Development Kit (JDK)** 11 или выше
- **Android Studio** (последняя версия)
- **Android SDK** (устанавливается через Android Studio)
- **Android Emulator** (AVD - Android Virtual Device)

### Для iOS (только macOS):
- **Xcode** 14 или выше
- **Xcode Command Line Tools**
- **CocoaPods** (`sudo gem install cocoapods`)
- **iOS Simulator** (устанавливается с Xcode)

---

## 🔧 Установка зависимостей

### Шаг 1: Перейдите в папку проекта

```bash
cd ClientApp
```

> **Примечание:** Если у вас есть две папки `ClientApp` и `ClientApp/ClientApp`, используйте ту, где есть `package.json` с зависимостями.

### Шаг 2: Установите зависимости

**С помощью npm:**
```bash
npm install
```

**Или с помощью yarn:**
```bash
yarn install
```

> ⏱️ Это может занять несколько минут при первой установке.

---

## 📱 Запуск на Android

### Шаг 1: Установка Android Studio

1. Скачайте и установите [Android Studio](https://developer.android.com/studio)
2. При первом запуске выберите **Standard Installation**
3. Дождитесь загрузки Android SDK и компонентов

### Шаг 2: Настройка Android SDK

1. Откройте Android Studio
2. Перейдите в **File → Settings** (или **Android Studio → Preferences** на Mac)
3. **Appearance & Behavior → System Settings → Android SDK**
4. Убедитесь, что установлены:
   - ✅ Android SDK Platform (последняя версия)
   - ✅ Android SDK Build-Tools
   - ✅ Android Emulator
   - ✅ Intel x86 Emulator Accelerator (HAXM installer) - для Windows

### Шаг 3: Создание Android эмулятора (AVD)

1. В Android Studio: **Tools → Device Manager**
2. Нажмите **Create Device**
3. Выберите устройство (например, **Pixel 5**)
4. Выберите системный образ (рекомендуется **API 33** или выше)
5. Нажмите **Finish**
6. Запустите эмулятор, нажав кнопку ▶️

> 💡 **Совет:** Держите эмулятор запущенным перед запуском приложения.

### Шаг 4: Настройка переменных окружения (опционально)

**Windows:**
```powershell
# Добавьте в переменные окружения PATH:
%LOCALAPPDATA%\Android\Sdk\platform-tools
%LOCALAPPDATA%\Android\Sdk\emulator
```

**macOS/Linux:**
```bash
# Добавьте в ~/.bashrc или ~/.zshrc:
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/platform-tools
```

### Шаг 5: Проверка подключения

Откройте новый терминал и выполните:
```bash
adb devices
```

Должен отобразиться ваш эмулятор:
```
List of devices attached
emulator-5554   device
```

### Шаг 6: Запуск приложения

**Терминал 1 - Metro Bundler:**
```bash
cd ClientApp
npm start
# или
yarn start
```

Дождитесь сообщения:
```
Metro waiting on exp://...
```

**Терминал 2 - Запуск приложения:**
```bash
cd ClientApp
npm run android
# или
yarn android
```

> ⏱️ Первый запуск может занять 5-10 минут (компиляция нативного кода).

### Шаг 7: Результат

Приложение должно открыться в Android эмуляторе! 🎉

---

## 🍎 Запуск на iOS (только macOS)

### Шаг 1: Установка Xcode

1. Установите **Xcode** из App Store
2. Откройте Xcode и примите лицензионное соглашение
3. Установите дополнительные компоненты при запросе

### Шаг 2: Установка CocoaPods

```bash
sudo gem install cocoapods
```

Проверьте установку:
```bash
pod --version
```

### Шаг 3: Установка iOS зависимостей

```bash
cd ClientApp/ios
pod install
cd ../..
```

> ⏱️ Это может занять несколько минут.

### Шаг 4: Запуск iOS симулятора

**Вариант 1 - Через Xcode:**
1. Откройте Xcode
2. **Xcode → Open Developer Tool → Simulator**
3. Выберите устройство (например, **iPhone 14**)

**Вариант 2 - Через командную строку:**
```bash
open -a Simulator
```

### Шаг 5: Запуск приложения

**Терминал 1 - Metro Bundler:**
```bash
cd ClientApp
npm start
# или
yarn start
```

**Терминал 2 - Запуск приложения:**
```bash
cd ClientApp
npm run ios
# или
yarn ios
```

> ⏱️ Первый запуск может занять 5-10 минут.

### Шаг 6: Результат

Приложение должно открыться в iOS симуляторе! 🎉

---

## 🔄 Полезные команды

### Перезагрузка приложения

**Android:**
- Нажмите `R` дважды в терминале Metro
- Или встряхните устройство → **Reload**
- Или `Ctrl + M` (Windows/Linux) / `Cmd + M` (Mac) → **Reload**

**iOS:**
- Нажмите `Cmd + R` в симуляторе
- Или встряхните устройство → **Reload**

### Очистка кэша

```bash
# Очистка Metro bundler кэша
npm start -- --reset-cache

# Очистка Android сборки
cd android
./gradlew clean
cd ..

# Очистка iOS сборки
cd ios
rm -rf build
pod deintegrate
pod install
cd ..
```

### Просмотр логов

**Android:**
```bash
npx react-native log-android
```

**iOS:**
```bash
npx react-native log-ios
```

---

## 🐛 Решение проблем

### Проблема: "SDK location not found"

**Решение:**
Создайте файл `android/local.properties`:
```properties
sdk.dir=C:\\Users\\ВашеИмя\\AppData\\Local\\Android\\Sdk
```
(Windows) или
```properties
sdk.dir=/Users/ВашеИмя/Library/Android/sdk
```
(macOS)

### Проблема: "Command not found: adb"

**Решение:**
Добавьте Android SDK platform-tools в PATH (см. Шаг 4 для Android)

### Проблема: "Unable to load script"

**Решение:**
```bash
# Очистите кэш и перезапустите
npm start -- --reset-cache
```

### Проблема: "Metro bundler не запускается"

**Решение:**
```bash
# Убейте процесс на порту 8081
# Windows:
netstat -ano | findstr :8081
taskkill /PID <PID> /F

# macOS/Linux:
lsof -ti:8081 | xargs kill -9

# Затем перезапустите
npm start
```

### Проблема: "Gradle build failed"

**Решение:**
```bash
cd android
./gradlew clean
cd ..
npm run android
```

### Проблема: "iOS build failed - Pod install error"

**Решение:**
```bash
cd ios
rm -rf Pods Podfile.lock
pod deintegrate
pod install
cd ..
```

### Проблема: "Эмулятор не запускается"

**Решение:**
- Убедитесь, что включена виртуализация в BIOS (для Windows)
- Проверьте, что HAXM установлен (Windows)
- Попробуйте создать новый AVD с другими настройками

### Проблема: "Приложение не подключается к серверу"

**Решение:**
1. Проверьте файл `env.js` - должен содержать правильный URL сервера
2. Убедитесь, что backend сервисы запущены
3. Для Android эмулятора используйте `10.0.2.2` вместо `localhost`
4. Проверьте настройки файрвола

---

## 📝 Дополнительная информация

### Структура проекта

```
ClientApp/
├── android/          # Android нативный код
├── ios/              # iOS нативный код
├── Screens/          # Экраны приложения
├── Components/       # Компоненты
├── services/         # API сервисы
├── App.js            # Главный файл
└── package.json      # Зависимости
```

### Конфигурация

- **API URL**: Настроен в `env.js` (по умолчанию: `http://87.228.9.225`)
- **Порт Metro**: 8081 (по умолчанию)
- **Порт Android Debug Bridge**: 5037

### Полезные ссылки

- [React Native документация](https://reactnative.dev/docs/getting-started)
- [Android Studio](https://developer.android.com/studio)
- [Xcode](https://developer.apple.com/xcode/)
- [Troubleshooting React Native](https://reactnative.dev/docs/troubleshooting)

---

## ✅ Чеклист первого запуска

- [ ] Установлен Node.js >= 18
- [ ] Установлен Android Studio (для Android) или Xcode (для iOS)
- [ ] Создан эмулятор/симулятор
- [ ] Установлены зависимости (`npm install`)
- [ ] Установлены iOS pods (только для iOS: `cd ios && pod install`)
- [ ] Запущен Metro bundler (`npm start`)
- [ ] Запущено приложение (`npm run android` или `npm run ios`)

---

**Удачи с запуском! 🚀**

Если возникнут проблемы, проверьте раздел [Решение проблем](#решение-проблем) или обратитесь к официальной документации React Native.
