# 🔧 Исправление версии Java для Gradle

## ❌ Проблема
```
Unsupported class file major version 69
```

**Причина:** Java 25 слишком новая для Gradle 8.3. Gradle 8.3 поддерживает только до Java 19.

---

## ✅ Решение: Установить Java 17 LTS

### Вариант 1: Установить Java 17 рядом с Java 25 (рекомендуется)

**1. Скачайте Java 17:**
- **Eclipse Temurin (OpenJDK):** https://adoptium.net/temurin/releases/?version=17
  - Выберите: Windows x64, JDK, .msi installer
- **Или Oracle JDK:** https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html

**2. Установите Java 17** (например, в `C:\Program Files\Eclipse Adoptium\jdk-17.0.13+11`)

**3. Установите JAVA_HOME на Java 17:**

**Через PowerShell (как администратор):**
```powershell
[System.Environment]::SetEnvironmentVariable('JAVA_HOME', 'C:\Program Files\Eclipse Adoptium\jdk-17.0.13+11', [System.EnvironmentVariableTarget]::User)
```

**Или через GUI:**
1. Win + R → `sysdm.cpl` → Enter
2. "Дополнительно" → "Переменные среды"
3. Измените `JAVA_HOME` на путь к Java 17
4. В `Path` убедитесь, что `%JAVA_HOME%\bin` идет ПЕРВЫМ (или удалите старые пути к Java)

**4. Перезапустите терминал** и проверьте:
```powershell
$env:JAVA_HOME
java -version
```

Должно показать Java 17.

---

### Вариант 2: Обновить Gradle до версии, поддерживающей Java 25

**Обновите Gradle в `ClientApp/android/gradle/wrapper/gradle-wrapper.properties`:**

```properties
distributionUrl=https\://services.gradle.org/distributions/gradle-8.10-all.zip
```

Gradle 8.10 поддерживает Java 25, но это может вызвать другие проблемы совместимости.

**⚠️ Не рекомендуется** - лучше использовать Java 17.

---

### Вариант 3: Использовать локальную Java 17 только для этого проекта

**В `ClientApp/android/gradle.properties` добавьте:**

```properties
org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-17.0.13+11
```

(Замените путь на ваш путь к Java 17)

Это переопределит JAVA_HOME только для Gradle в этом проекте.

---

## 🎯 Рекомендуемое решение

**Используйте Java 17 LTS** - это стандарт для:
- React Native
- Android разработки
- Gradle 8.3

**Шаги:**
1. ✅ Скачайте и установите Java 17
2. ✅ Установите JAVA_HOME на Java 17
3. ✅ Перезапустите терминал
4. ✅ Проверьте: `java -version` (должна быть 17)
5. ✅ Запустите: `npm run android`

---

## 📝 Проверка после установки

```powershell
# Проверка JAVA_HOME
$env:JAVA_HOME

# Проверка версии Java
java -version
# Должно показать: java version "17.0.x"

# Проверка компилятора
javac -version
# Должно показать: javac 17.0.x
```

---

## ⚠️ Важно

- Java 25 можно оставить установленной
- Просто переключите JAVA_HOME на Java 17
- Для других проектов можно использовать Java 25, но для React Native нужна 17

---

## 🚀 После исправления

```bash
cd ClientApp
npm run android
```

Ошибка "Unsupported class file major version 69" должна исчезнуть! ✅
