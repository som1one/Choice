# ✅ План устранения проблемы JAVA_HOME

## 🎯 Цель
Установить JAVA_HOME на правильный путь: `D:\jdk-25.0.2`

---

## 📋 Шаги выполнения

### Шаг 1: Установить JAVA_HOME через PowerShell (уже выполнено ✅)

```powershell
[System.Environment]::SetEnvironmentVariable('JAVA_HOME', 'D:\jdk-25.0.2', [System.EnvironmentVariableTarget]::User)
```

**Статус:** ✅ Выполнено

---

### Шаг 2: Проверить, что путь правильный

```powershell
# Проверка существования JDK
Test-Path "D:\jdk-25.0.2\bin\javac.exe"
# Должно вернуть: True

# Проверка версии Java
java -version
javac -version
```

**Статус:** ✅ Проверено - путь существует

---

### Шаг 3: Перезапустить терминал ⚠️ ВАЖНО!

**Обязательно закройте и откройте терминал заново**, чтобы изменения вступили в силу.

**Или в текущем терминале:**
```powershell
$env:JAVA_HOME = 'D:\jdk-25.0.2'
```

---

### Шаг 4: Проверить JAVA_HOME после перезапуска

```powershell
$env:JAVA_HOME
```

**Ожидаемый результат:** `D:\jdk-25.0.2`

---

### Шаг 5: Добавить в PATH (если нужно)

Проверьте, что `%JAVA_HOME%\bin` есть в PATH:

```powershell
$env:PATH -split ';' | Select-String "Java"
```

Если нет, добавьте:
```powershell
$currentPath = [System.Environment]::GetEnvironmentVariable('Path', [System.EnvironmentVariableTarget]::User)
$newPath = "$currentPath;D:\jdk-25.0.2\bin"
[System.Environment]::SetEnvironmentVariable('Path', $newPath, [System.EnvironmentVariableTarget]::User)
```

---

### Шаг 6: Проверить работу

```powershell
# Проверка JAVA_HOME
echo $env:JAVA_HOME

# Проверка Java
java -version

# Проверка компилятора
javac -version
```

---

### Шаг 7: Запустить приложение

```bash
cd ClientApp
npm run android
```

---

## 🔍 Проверка результата

После выполнения всех шагов:

✅ `$env:JAVA_HOME` показывает `D:\jdk-25.0.2`  
✅ `java -version` работает  
✅ `javac -version` работает  
✅ `npm run android` запускается без ошибки JAVA_HOME  

---

## ⚠️ Если не работает

### Вариант 1: Установить системную переменную (требует прав администратора)

```powershell
# Запустите PowerShell как администратор
[System.Environment]::SetEnvironmentVariable('JAVA_HOME', 'D:\jdk-25.0.2', [System.EnvironmentVariableTarget]::Machine)
```

### Вариант 2: Через GUI (без прав администратора)

1. Win + R → `sysdm.cpl` → Enter
2. Вкладка "Дополнительно" → "Переменные среды"
3. В "Переменные пользователя" найдите или создайте `JAVA_HOME`
4. Установите значение: `D:\jdk-25.0.2`
5. Нажмите OK
6. Перезапустите терминал

### Вариант 3: Временная установка в текущей сессии

```powershell
$env:JAVA_HOME = 'D:\jdk-25.0.2'
```

Работает только в текущем терминале.

---

## 📝 Текущий статус

- ✅ JAVA_HOME установлен на `D:\jdk-25.0.2`
- ✅ Путь к JDK проверен и существует
- ⚠️ **Нужно перезапустить терминал** для применения изменений
- ⏳ После перезапуска проверить и запустить приложение

---

## 🚀 Быстрая команда для проверки

После перезапуска терминала выполните:

```powershell
$env:JAVA_HOME
cd ClientApp
npm run android
```

Если всё правильно настроено, приложение должно запуститься! 🎉
